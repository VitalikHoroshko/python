import math

# Введення трьох невід'ємних цілих чисел a, z, x
a = int(input("Введіть ціле число a: "))
z = int(input("Введіть ціле число z: "))
x = int(input("Введіть ціле число x: "))

# Обчислення виразу за формулою
Y = (1 + z) * math.sqrt(x) / (a - math.sqrt(1 / (1 + x)))

# Виведення результату
print("Результат обчислення формули: ", Y)
